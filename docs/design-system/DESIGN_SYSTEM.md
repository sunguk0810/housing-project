# 집콕신혼 디자인 시스템 명세

## 0. 문서 정보

| 항목 | 값 |
|------|------|
| 버전 | 1.0 |
| 상태 | Active |
| 최종 수정 | 2026-02-14 |
| SoT 관계 | 본 문서는 디자인 시스템 **상세 명세**의 SoT이다. 요약 토큰은 `PHASE1_design.md` S7에 기재하고, 상세는 본 문서를 참조한다. |

> **SoT 참조**: FR/NFR/법무 체크리스트 → `docs/PHASE0_ground.md` 참조 | DB 스키마 → `docs/PHASE1_design.md` S2 참조 | 스코어링 로직 → `docs/PHASE1_design.md` S4 참조

---

## 1. 설계 원칙

### 1.1 핵심 UX 원칙 (7대)

| # | 원칙 | 요약 | 핵심 구현 규칙 |
|---|------|------|--------------|
| 1 | **비동기 협업 우선** | 커플 탐색은 각자 따로, 짧게, 반복적 | 공유 찜 목록 + "둘 다 찜/한 명만 찜" 매칭 표시 |
| 2 | **3입력 내 첫 가치 전달** | 소득·자산·대출 3개 입력만으로 예산 범위 즉시 표시 | 온보딩 완료 후 결과 화면(`/results`)으로 직접 라우팅 |
| 3 | **지도가 곧 검색** | 바운딩 박스 자동 갱신 + 가격 라벨 핀 + 3단 바텀시트 | 지도 팬/줌 시 별도 "검색" 버튼 없이 자동 재검색 |
| 4 | **점수는 이야기** | 0-100 점수 + 라벨 + 요인 분해 + 백분위 4층 구조 | ⓘ → 1줄 요약 → 요인 기여도 → 전체 방법론 3단계 계층적 공개 |
| 5 | **실거래가가 최고의 신뢰** | 호가 옆에 동일 조건 실거래 이력 항상 병기 | 모든 가격 데이터에 국토교통부 출처·기준일 인라인 태그 |
| 6 | **점진적 몰입** | 등록 없이 탐색 → 카카오 로그인 → 파트너 초대 | 결과 확인까지 로그인 미요구 (찜/알림/공유 시점에 유도) |
| 7 | **신뢰 신호는 적지만 깊게** | 과도한 배지 나열은 역효과 | 검증된 소수의 신뢰 장치(실거래가·출처 태그·🔒 비저장)를 콘텐츠에 통합 |

> **출처**: `docs/legacy_docs/references/design_research/step2_ux_principle.md`, `design_brief.md`

### 1.2 컴플라이언스 원칙

| 원칙 | 설명 |
|------|------|
| 정보 분석 플랫폼 포지셔닝 | 부동산 중개·알선·자문이 아닌 정보 분석 플랫폼임을 일관되게 전달 |
| 면책 5접점 배치 | 랜딩 푸터 → 입력 배너 → 결과 메타 → 외부 링크 모달 → 이용약관 |
| 금지 문구 통제 | "추천" 단독 사용 금지 → "분석 결과 안내"로 대체 (상세: §5 참조) |
| 출처 필수 표기 | 모든 공공데이터 사용 시 출처 + 기준일 인라인 표기 |
| 개인정보 비저장 | 입력 정보는 세션 종료 시 즉시 삭제 (→ PHASE0 NFR-1 참조) |

> **출처**: `docs/PHASE0_ground.md` §4 법무 체크리스트, `docs/legacy_docs/references/design_research/step6_compliance_ux_design_guide.md`

---

## 2. 디자인 토큰

> **구현 파일**: `docs/design-system/design-tokens.css` — 런타임 `:root` 변수 + Tailwind v4 `@theme` 정의

### 2.1 컬러 시스템

#### Primary Scale — 웜 틸 블루 (Warm Teal Blue)

| 단계 | HEX | 용도 |
|------|---------|------|
| 50 | `#CFFAFE` | 밝은 배경, 태그 |
| 100 | `#A5F3FC` | 서브 배경 |
| 200 | `#67E8F9` | 호버 상태 |
| 300 | `#22D3EE` | 비활성 요소 |
| 400 | `#06B6D4` | 보조 버튼, 다크모드 Primary |
| **500** | **`#0891B2`** | **주 브랜드 색상** |
| 600 | `#0E7490` | 호버/액티브 |
| 700 | `#155E75` | 텍스트 사용 (AA✅ ~5.5:1) |
| 800 | `#164E63` | 강조 텍스트 |
| 900 | `#0C4A6E` | 최 어두운 텍스트 |

#### Accent / Secondary — 코랄 오렌지

| 용도 | HEX | 비고 |
|------|---------|------|
| CTA 버튼·하이라이트 | `#F97316` | 코랄 오렌지 |
| 텍스트용 어두운 변형 | `#C2410C` | AA 준수 |

#### Neutral — Stone 계열 (웜 뉴트럴)

| 용도 | HEX | 비고 |
|------|---------|------|
| 배경 (순백 대체) | `#FAFAF9` | Stone 50 |
| Surface | `#FFFFFF` | 카드/모달 배경 |
| 보더 | `#E7E5E4` | Stone 300 |
| 보조 텍스트 | `#78716C` | Stone 500 |
| 비활성 텍스트 | `#A8A29E` | Stone 400 |
| 주요 텍스트 (순흑 대체) | `#1C1917` | 웜 다크 |

#### Semantic 컬러

| 용도 | HEX | 비고 |
|------|---------|------|
| Success | `#059669` | Emerald 600 |
| Warning | `#D97706` | Amber 600 |
| Error | `#DC2626` | Red 600 |

#### Score 5등급 — 블루-오렌지 다이버징 (색맹 안전)

> ⚠️ 적-녹 스케일 사용 **금지** (남성 ~8% 적녹색맹). 반드시 텍스트 라벨(A+/A/B/C/D) 병행 (WCAG 1.4.1)

| 등급 | 점수 범위 | 한국어 라벨 | 배지 | HEX |
|------|----------|----------|------|---------|
| Excellent | 80–100 | 매우 좋음 | A+ | `#1565C0` (딥 블루) |
| Good | 60–79 | 좋음 | A | `#2196F3` (블루) |
| Average | 40–59 | 보통 | B | `#B0BEC5` (뉴트럴 그레이블루) |
| Below Avg | 20–39 | 미흡 | C | `#EF6C00` (딥 오렌지) |
| Poor | 0–19 | 부족 | D | `#D84315` (딥 오렌지) |

#### Safety 전용 색상 (빨강 사용 금지)

| 조건 | 색상 | HEX |
|------|------|---------|
| 80%+ (충분) | 파랑 | `#1976D2` |
| 60–79% (보통) | 앰버 | `#FFC107` |
| 60% 미만 (부족) | 진회색 | `#757575` |

> **가드레일**: "위험" 표현에 빨강 사용 금지. "안전 편의시설 현황"으로 표현 (→ §5.2 참조)

#### Semantic Token Layer — Light/Dark 모드 대응

| 토큰 | Light 모드 | Dark 모드 |
|------|-----------|-----------|
| `--color-surface` | `#FFFFFF` | `#0F172A` |
| `--color-surface-elevated` | `#FAFAF9` | `#1E293B` |
| `--color-on-surface` | `#1C1917` | `#F1F5F9` |
| `--color-on-surface-muted` | `#78716C` | `#94A3B8` |
| `--color-border` | `#E7E5E4` | `#334155` |
| `--color-primary` | `#0891B2` (brand-500) | `#06B6D4` (brand-400) |
| `--color-surface-sunken` | `#F5F5F4` | `#0C1222` |
| `--color-primary-hover` | `#0E7490` | `#22D3EE` |
| `--color-on-primary` | `#FFFFFF` | `#0F172A` |

#### WCAG 대비 비율 가이드

| 조합 | 대비 비율 | 기준 |
|------|----------|------|
| `#0891B2` on `#FFFFFF` | ~3.8:1 | 큰 텍스트/UI OK (3:1), 본문 텍스트에는 `#155E75` 사용 |
| `#155E75` on `#FFFFFF` | ~5.5:1 | AA 통과 |
| `#C2410C` on `#FFFFFF` | ~4.7:1 | AA 통과 (accent 텍스트용) |
| `#1C1917` on `#FAFAF9` | ~15:1 | AAA 통과 |
| `#78716C` on `#FAFAF9` | ~4.5:1 | AA 통과 |

### 2.2 타이포그래피

#### 서체: Pretendard Variable

- **선정 근거**: 정부 KRDS 채택, 한글 11,172자 완성형, Inter 기반 숫자 렌더링, SIL OFL 라이선스
- **CDN**: `https://cdn.jsdelivr.net/gh/orioncactus/pretendard@v1.3.9/dist/web/variable/pretendardvariable-dynamic-subset.min.css`
- **font-family**: `"Pretendard Variable", "Pretendard", "Noto Sans KR", system-ui, -apple-system, sans-serif`

#### 7단계 Type Scale

| 역할 | 크기 | rem | Weight | line-height | letter-spacing |
|------|------|-----|--------|-------------|---------------|
| Caption / Label | 12px | 0.75rem | Medium (500) | 1.4 | -0.02em |
| Body Small | 14px | 0.875rem | Regular (400) | 1.5 | -0.02em |
| **Body** | **16px** | **1rem** | **Regular (400)** | **1.6** | **-0.02em** |
| Subtitle | 18px | 1.125rem | SemiBold (600) | 1.4 | -0.02em |
| Title | 20px | 1.25rem | SemiBold (600) | 1.3 | -0.03em |
| Heading | 24px | 1.5rem | Bold (700) | 1.25 | -0.03em |
| Display | 32px | 2rem | Bold (700) | 1.25 | -0.04em |

#### 한글 타이포그래피 규칙

- **최소 크기**: 한글은 획수가 많아 영문보다 가독성이 낮음. 본문 최소 13–14px, KRDS 권장 16px
- **가격 표시**: `font-variant-numeric: tabular-nums` (고정폭 숫자), weight를 본문보다 1단계 두껍게
- **₩ 기호**: 쉼표 전후 여백 주의, 만원 단위 표기 시 "3억 2,000만"

### 2.3 간격 & 레이아웃

#### 4px 기본 단위

| Tailwind 단위 | px | 용도 |
|---------------|-----|------|
| `1` | 4px | 최소 간격 |
| `2` | 8px | 아이콘-텍스트 간격 |
| `3` | 12px | 리스트 아이템 간격 |
| `4` | 16px | 카드 패딩 |
| `5` | 20px | 섹션 내부 간격 |
| `6` | 24px | 섹션 간 간격 |
| `8` | 32px | 큰 섹션 간격 |
| `12` | 48px | 페이지 섹션 간격 |

#### 터치 타겟

| 기준 | 최소 크기 | 비고 |
|------|----------|------|
| WCAG 2.5.5 | 44 × 44px | 시각적 크기와 별도로 터치 영역 확보 |
| Google Material | 48 × 48dp | 권장 최적 크기 |

#### Border Radius

| 요소 | 값 | Tailwind |
|------|------|---------|
| 카드 | 16px | `rounded-2xl` |
| 버튼 | 12px | `rounded-xl` |
| 바 차트 / 필 바 | 99px (pill) | `rounded-full` |
| 입력 필드 | 12px | `rounded-xl` |
| 배지 / 태그 | 6px | `rounded-md` |
| 마커 프리뷰 | 12px | `rounded-xl` |

#### Shadow 체계

| 레벨 | CSS | 용도 |
|------|-----|------|
| sm | `0 1px 2px rgba(0,0,0,0.05)` | 배지, 태그 |
| base | `0 2px 8px rgba(0,0,0,0.08)` | 카드 기본 |
| md | `0 4px 12px rgba(0,0,0,0.10)` | 호버 카드 |
| lg | `0 8px 24px rgba(0,0,0,0.12)` | 모달, 바텀시트 |
| marker | `0 2px 6px rgba(0,0,0,0.30)` | 지도 마커 |
| marker-selected | `0 4px 16px {color}40` | 선택된 마커 |

### 2.4 반응형

#### 브레이크포인트

| 이름 | px | 용도 |
|------|------|------|
| xs | 360px | 소형 모바일 (커스텀 추가) |
| 디자인 기준 | **390px** | 기본 디자인 화면. 360–430px 가변 대응 |
| sm | 640px | 태블릿 세로 |
| md | 768px | 태블릿 가로 |
| lg | **1024px** | 데스크톱 — **스플릿 뷰 전환점** |
| xl | 1280px | 대형 데스크톱 |

#### 데스크톱 스플릿 레이아웃

| 비율 | 채택 | 비고 |
|------|------|------|
| **60% 지도 / 40% 리스트** | ✅ 채택 | Zillow/Redfin 표준, 지도 중심 |
| 최소 리스트 패널 | 480px | 카드 1열 기준 320–400px, 정보 충분 표시 480px+ |

#### 모바일 → 데스크톱 전환

- 1024px 미만: 3단 바텀시트 + 전체 너비 지도
- 1024px 이상: 좌측 리스트 패널(40%) + 우측 지도(60%)

### 2.5 애니메이션 & 트랜지션

#### 전체 타이밍 레퍼런스

| # | 인터랙션 | 지속시간 | 이징 |
|---|---------|---------|------|
| 1 | **Score count-up** | 800–1200ms | `easeOutExpo` `cubicBezier(0.16, 1, 0.3, 1)` |
| 2 | **Bar expand** | 500ms+ | `ease-out` |
| 3 | **Bottom sheet snap** | 300–400ms | `spring(stiffness: 400, damping: 30)` |
| 4 | **Marker tap preview** | 200–300ms | `cubicBezier(0.33, 1, 0.68, 1)` |
| 5 | **Skeleton shimmer** | 1500ms 반복 | `linear` |
| 6 | **Toast** | 200ms in → 2000ms hold → 150ms out | `ease-out` |
| 7 | **Marker selection scale** | 150ms | `spring(stiffness: 400, damping: 25)` |
| 8 | **Compare bar slide-up** | 300ms | `spring(stiffness: 300, damping: 30)` |
| 9 | **Stagger children** | 100ms 간격 | sequential |
| 10 | **Analysis loading stages** | 1200–1800ms 각 | sequential fade |

#### Progressive Reveal Sequence (점수 로딩)

| 단계 | 시간 | 내용 | 이징 |
|------|------|------|------|
| 1 | 0–300ms | 스켈레톤 shimmer | linear, 1500ms 반복 |
| 2 | 300–500ms | 종합 점수 카운트업 (0→N) | easeOutExpo |
| 3 | 500–800ms | 카테고리 점수 순차 fade-in | stagger 100ms |
| 4 | 800ms+ | 프로그레스 바 확장 | ease-out |

#### 분석 중 시퀀스 (온보딩 Step 5)

| 순서 | 아이콘 | 텍스트 | 지속시간 |
|------|-------|--------|---------|
| 1 | 🏘️ | 입력 조건 분석 중… | 1200ms |
| 2 | 🚇 | 통근 경로 계산 중… | 1400ms |
| 3 | 📊 | 예산 적합도 시뮬레이션 중… | 1600ms |
| 4 | 🗺️ | 조건 부합 단지 탐색 중… | 1800ms |

---

## 3. 컴포넌트 명세

### 3.1 스코어 시각화

#### CircularGauge (원형 게이지)

종합 점수 1개를 강조하는 SVG 게이지.

| 속성 | 카드 | 상세 Hero | 미니 |
|------|------|----------|------|
| 크기 | 64px | 80–96px | 36–48px |
| 숫자 크기 | 20px Bold | 32px Bold | 14px SemiBold |
| 스트로크 너비 | 4px | 6px | 3px |
| 트랙 색상 | `brand-100` | `brand-100` | `brand-100` |
| 필 색상 | Score 등급 컬러 | Score 등급 컬러 | Score 등급 컬러 |
| 애니메이션 | count-up 800ms easeOutExpo | count-up 1200ms easeOutExpo | 없음 |

#### ScoreBar (수평 바 차트)

카테고리별 점수를 수평 바로 표현. 375px 모바일에서 가장 효과적.

| 속성 | 값 |
|------|------|
| 높이 | 8px (compact: 6px) |
| 코너 | `rounded-full` (pill) |
| 트랙 색상 | `neutral-200` (#E7E5E4) |
| 필 색상 | Score 등급 컬러 |
| 바 간격 | 14px |
| 너비 | 전체 사용 가능 너비 (양쪽 16px 패딩 제외) |
| 애니메이션 | width transition 500ms ease-out |

#### ScoreBadge (점수 배지)

| 변형 | 크기 | 배경 | 텍스트 |
|------|------|------|--------|
| sm | 24px | Score 등급색 / 10% | Score 등급색, Caption weight |
| md | 32px | Score 등급색 / 10% | Score 등급색, Body-sm Bold |
| lg | 48px | Score 등급색 / 10% | Score 등급색, Title Bold |

#### RadarChart (레이더 차트)

- **용도**: 2–3개 단지 비교 **전용** (3개 이상 겹치면 가독성 급락)
- **최소 크기**: 200 × 200px
- **축 수**: 4축 (통근·보육·안전·가성비) 또는 5축
- **라이브러리**: Recharts
- **단지 색상**: `#0891B2` / `#F97316` / `#8B5CF6`
- **채움 투명도**: 10%, 스트로크 2px

### 3.2 카드

#### PropertyCard (단지 카드)

결과 리스트의 단위 카드.

| 영역 | 정보 | 스타일 |
|------|------|--------|
| 좌상단 | 순위 배지 (1, 2, 3…) | 상위 3: 코랄(`#F97316`) 배경 + 흰 텍스트, 이하: 회색 배경 |
| 단지명 | 배지 오른쪽 | 16px Bold (700) |
| 우상단 | 종합 점수 CircularGauge | 64px SVG |
| 정보 행 1 | 주소 | 12px 보조 텍스트 |
| 정보 행 2 | 세대수·준공·면적 | 11px dot 구분, 비활성 색상 |
| 가격 | 매매/전세 실거래가 | 13px Bold/SemiBold |
| 점수 그리드 | 예산·통근·보육·안전 | 2×2 그리드, 아이콘+라벨+등급색 숫자 |
| 하단 | 직장1·2 통근시간 + "비교 추가" 버튼 | 12px + 아웃라인 버튼 |
| 카드 스타일 | `rounded-2xl` `p-4` `bg-surface` `border border-border` `shadow-sm` | |

#### ComparisonCard (비교 카드)

- 나란히 비교 테이블: 행 단위 비교
- 최고 점수 셀: `brand-500/8%` 배경 + "더 좋음" 하이라이트
- 가격 항목: 하이라이트 **미적용** (저가가 반드시 좋은 것은 아님)

#### MiniPreviewCard (마커 탭 프리뷰)

| 속성 | 값 |
|------|------|
| 크기 | 280–320px × 100–140px |
| 내용 | 썸네일(120×80px) + 가격(Bold) + 점수(배지) + 주소(1줄) |
| 진입 | `translateY(20px) → 0` + `opacity 0→1`, 200–300ms |
| 위치 | 마커 위. 화면 상단 근접 시 아래로, 좌우 근접 시 수평 오프셋 |

#### CardSelector (정렬 칩)

- 종합 점수순(기본) / 예산 적합도순 / 통근 시간순
- 수평 칩 배열, 선택 시 `#0891B2` 배경 + 흰 텍스트
- 미선택: 아웃라인 스타일

### 3.3 네비게이션

#### BottomNav (하단 탭 바)

| 속성 | 값 |
|------|------|
| 탭 수 | 5 (홈 / 검색 / 지도 / 비교 / MY) |
| 높이 | 56px + `env(safe-area-inset-bottom)` |
| 활성 색상 | `#0891B2` + Bold |
| 비활성 색상 | `#9CA3AF` + Regular |
| 아이콘 | 🏠 / 🔍 / 🗺️ / 📊 / 👤 |
| 비교 탭 | 뱃지로 담긴 수 표시 |
| 숨김 동작 | `/complex/[id]`, `/results` 스크롤 다운 시 숨김 → 업 시 복귀 |

#### BottomSheet (3단 바텀시트)

넌모달: 시트가 올라와 있어도 지도 인터랙션 항상 가능.

| 상태 | 화면 비율 | 높이 | 표시 콘텐츠 |
|------|----------|------|-----------|
| Peek | ~25% | 100–160px | 드래그 핸들(40×4px) + 검색바 + 매물 수 |
| Half | ~50% | 350–400px | 리스트 카드 2–3개 |
| Expanded | ~90% | 상단 safe area 제외 | 전체 리스트 스크롤 + 정렬/필터 |

- 드래그 핸들: 40 × 4px, 라운드, `#A8A29E`
- 트랜지션: 300–400ms `spring(stiffness: 400, damping: 30)`
- 구현: Vaul (shadcn/ui Drawer, Radix 기반) 또는 CSS scroll snap 기반

#### CompareBar (비교 바)

| 속성 | 값 |
|------|------|
| 위치 | 하단 sticky (바텀 탭 위) |
| 높이 | 64–80px |
| 패딩 | `pb-[max(10px,env(safe-area-inset-bottom))]` |
| 최대 항목 | 모바일 3개, 데스크톱 4개 |
| 초과 시 | 토스트 "최대 3개까지 비교 가능합니다" |
| 진입 | slide-up 300ms `spring(stiffness: 300, damping: 30)` |

### 3.4 입력

#### AmountInput (금액 입력)

| 속성 | 값 |
|------|------|
| 타입 | 숫자 입력 + 빠른 입력 버튼 병행 |
| 빠른 버튼 | +1,000만 / +5,000만 / +1억 |
| 숫자 표시 | `tabular-nums`, 쉼표 구분, "만원" 단위 |
| 음수 금지 | 입력값 검증 |

#### AddressSearch (주소 검색)

| 속성 | 값 |
|------|------|
| API | Kakao 주소 검색 API (PHASE1 S1 SoT) |
| 입력 | 텍스트 + 자동완성 |
| 필드 수 | 직장 1 (필수) + 직장 2 (선택) |
| 안내 | "두 번째 직장은 선택사항입니다" |

#### StepWizard (온보딩 위저드)

| 속성 | 값 |
|------|------|
| 스텝 수 | 5 (URL은 `/search` 단일 경로 내 서브스텝) |
| 프로그레스 | 4단계 인디케이터 (Step 5 = 분석 중) |
| 패턴 | "원 퀘스천 퍼 스크린" 기반, 유사 맥락 2개씩 묶음 |
| 소요 시간 | 총 ~2분 30초 목표 |

**스텝 구성**:

| Step | 제목 | 내용 | 소요 시간 |
|------|------|------|----------|
| 1 | 우리 부부는요 | 주거 형태(매매/전세/월세) + 자녀 계획 | ~25초 |
| 2 | 출퇴근 정보 | 직장1 + 직장2 주소 | ~40초 |
| 3 | 소득과 자산 | 합산 연봉 + 보유 현금 | ~30초 |
| 4 | 부채와 예산 | 기존 대출 + 월 주거비 | ~30초 |
| 5 | 분석 중… | 결과 전환 애니메이션 (4단계) | ~7초 |

**입력 패턴 선택**:

| 입력 항목 | 채택 패턴 |
|----------|----------|
| 주거 형태 (매매/전세/월세) | 이모지+라벨 카드 선택 (3개) |
| 자녀 계획 | 이모지+라벨 카드 선택 (3개) |
| 직장 주소 | 텍스트 입력 (자동완성) |
| 연봉·자산·대출·월비용 | 빠른 입력 버튼 + 직접 입력 병행 |

### 3.5 지도 — Kakao Maps JS SDK 고정

> **SoT**: PHASE1 S1에서 Kakao Maps JS SDK로 확정. Naver Maps 등 타 API 사용 금지.

#### MapMarker (3상태)

| 상태 | 배경 | 텍스트 | 크기 | 그림자 |
|------|------|--------|------|--------|
| Default | `#FFFFFF` | Score 등급 컬러 | 1.0x (30–34px, 터치영역 44–48px) | `0 2px 8px rgba(0,0,0,0.12)` |
| Selected/Hovered | Score 등급 컬러 | `#FFFFFF` | 1.15x | `0 4px 16px {color}40` |
| Visited | `#FFFFFF` (70% opacity) | Score 등급 컬러 | 1.0x | `0 1px 4px rgba(0,0,0,0.08)` |

- 선택 트랜지션: 150ms `spring(stiffness: 400, damping: 25)`
- 마커 위 점수 표시: 종합 점수 라벨 핀

#### Clustering (클러스터링)

| 줌 레벨 | 표시 단위 | 클러스터링 |
|---------|----------|-----------|
| 7–10 (도/시) | 지역별 대형 클러스터 | 원형 카운터 (40–60px) |
| 11–13 (구/동) | 중간 클러스터 | 매물 수 표시 |
| 14+ (단지/거리) | 개별 마커 | 가격 라벨 핀 |

- 줌 변경 시 debounce 200–300ms
- 뷰포트 내 마커만 렌더링

#### Legend (범례)

- Score 5등급 색상 범례
- 지도 우하단 배치, 반투명 배경

#### 지도-리스트 양방향 싱크

- 카드 hover → 마커 하이라이트
- 마커 클릭 → 카드 자동 스크롤
- 바텀시트 스와이프 → 마커 하이라이트

### 3.6 신뢰 & 컴플라이언스

#### TrustBadge (신뢰 배지)

| 변형 | 위치 | 구성 |
|------|------|------|
| Full | 금융 입력 폼 상단 (Step 3) | 인라인 배너: `brand-50` 배경, 🔒 아이콘 + "입력 정보는 분석 후 즉시 삭제됩니다" + 부연 텍스트 |
| Mini | 각 금융 입력 화면 하단 | 1줄: "이 금액은 분석 후 즉시 삭제됩니다" |

> **가드레일**: "개인정보를 안전하게 보호합니다" 대신 구체적 행동("분석 후 즉시 삭제") 명시

#### DataSourceTag (출처 태그)

| 아이콘 | 출처명 | 형태 |
|--------|--------|------|
| 📊 | 공공데이터 | `[📊 공공데이터 기반 분석 결과]` |
| 🚇 | ODsay | `[🚇 대중교통 경로 기준]` |
| 🏫 | 보육시설 | `[🏫 사회보장정보원]` |
| 📅 | 기준일 | `[기준일 2026.02.01]` |

- 결과 상단 메타 영역에 태그 배열
- 개별 데이터 항목 옆에도 인라인 표시 가능

#### ExternalLinkCTA (외부 링크 CTA)

**버튼**: `외부 매물 보러가기 ↗ 네이버 부동산`
- 하단 부가 텍스트: "외부 사이트로 이동합니다" (12px, 보조 색상)

**클릭 시 모달** (필수):
1. 이동 대상 사이트명 명시
2. 정보 제공 주체가 외부임 명시
3. 면책: "본 서비스는 정보 분석 플랫폼이며 부동산 중개 서비스가 아닙니다"
4. 확인/취소 양쪽 선택지

> **가드레일**: "바로 문의" / "바로 연결" / "전화하기" → "외부 페이지로 이동" 으로 대체

#### ConsentForm (동의 폼)

| 구분 | 항목 | 보유 기간 |
|------|------|----------|
| **[필수]** 개인정보 수집·이용 | 직장 권역, 예산 범위 | 세션 종료 시 즉시 삭제 |
| **[선택]** 정밀 분석 추가 | 보유 현금, 상세 소득 | 세션 종료 시 즉시 삭제 |
| **[필수]** 위치정보 이용 | 직장 주소 → 좌표 변환 | 좌표 미저장 |

- 필수 미체크 시 CTA 비활성
- 선택 동의 없이도 기본 분석 가능
- 입력 시작 직전 인라인 수집 (별도 페이지 이동 없음)

#### SafetySection (안전 편의시설 섹션)

- 섹션 타이틀: **"안전 편의시설 현황"** (절대 "치안 점수", "범죄율" 사용 금지)
- 지표: CCTV 수, 가로등 밀집도, 경찰서 거리, 비상벨 수 (시설 수량/거리)
- 비교: "서울시 평균 대비", "수도권 상위 N%"
- 색상: 충분=파랑, 보통=앰버, 부족=**진회색** (빨강 금지)
- **금지 표현**: "범죄율 높음", "위험 지역", "치안 열악", 원시 범죄 데이터, 순위형 비하

### 3.7 피드백

#### Toast (토스트 알림)

| 속성 | 값 |
|------|------|
| 진입 | slide-up 200ms ease-out |
| 유지 | 2000ms |
| 퇴장 | fade-out 150ms |
| 위치 | 하단 중앙, 바텀 탭 위 |
| 예시 | "비교 목록에 추가되었습니다", "최대 3개까지 비교 가능합니다" |

#### Tooltip (툴팁)

| 속성 | 값 |
|------|------|
| 트리거 | ⓘ 아이콘 탭 |
| 배경 | `#1C1917` (웜 다크) |
| 텍스트 | `#FFFFFF` |
| 내용 | "왜 이 정보가 필요한지" 1줄 해설 |
| 3단계 공개 | ⓘ → 1줄 요약 → 요인 기여도 → 전체 방법론 |

#### Skeleton (스켈레톤 로딩)

| 속성 | 값 |
|------|------|
| 배경 | `#E7E5E4` |
| 하이라이트 | `#FAFAF9` |
| 애니메이션 | shimmer sweep, linear, 1500ms 반복 |
| 적용 대상 | 카드, 리스트, 점수 영역 |

---

## 4. 페이지 패턴

### 4.1 사이트맵

```
🌐 / (홈·랜딩)
├── /search           조건 입력 (5스텝 위저드, 단일 경로)
├── /results          분석 결과 (지도 + 바텀시트 리스트)
├── /complex/[id]     단지 상세
├── /compare          단지 비교 (최대 3개)
├── /board/[id]       공유 보드 (커플 협업)
├── /guide            신혼부부 주거 가이드
├── /auth             로그인·회원가입·콜백
├── /mypage           마이페이지 (저장·이력·알림·파트너)
├── /terms            이용약관
├── /privacy          개인정보처리방침
└── /location-terms   위치정보 이용약관
```

### 4.2 렌더링 전략

| URL | 렌더링 | 인증 |
|-----|--------|------|
| `/` | SSG | 불필요 |
| `/search` | CSR | 불필요 |
| `/results` | SSR | 불필요 |
| `/complex/[id]` | ISR (1h) | 불필요 |
| `/compare` | CSR | 불필요 |
| `/board/[id]` | SSR | 쓰기🔐 / 읽기❌ |
| `/mypage/**` | CSR | 🔐 |

### 4.3 페이지별 레이아웃

#### Landing (`/`)

- 서비스 소개 + 핵심 가치 CTA
- 면책/포지셔닝 고지 (푸터 상시)
- "3입력으로 시작" CTA → `/search`

#### Onboarding (`/search`)

- 5스텝 위저드 (단일 URL)
- 프로그레스 인디케이터
- Sticky 하단 CTA
- 동의 수집 (인라인)
- 신뢰 배지 (Full + Mini)

#### Results (`/results`)

- 지도 뷰 + 리스트 뷰 전환/동시 표시
- 3단 바텀시트 (모바일)
- 스플릿 뷰 (데스크톱 1024px+)
- 정렬 칩 바
- 출처/기준일 메타 태그
- "더 보기" 버튼 (무한 스크롤 미채택)
- 상단 면책 메타 태그: `[📊 공공데이터 기반 분석 결과] [기준일 2026.02.01] [참고용 정보]`

#### Detail (`/complex/[id]`)

- Hero: 종합 점수 CircularGauge (80–96px)
- 카테고리별 ScoreBar
- 실거래가 이력 + 출처 태그
- 직장1·2 통근시간 카드
- 안전 편의시설 현황 (SafetySection)
- 외부 링크 CTA (ExternalLinkCTA)

#### Comparison (`/compare`)

- 나란히 비교 테이블
- RadarChart (2–3개)
- 행 단위 "더 좋음" 하이라이트
- 모바일: 스와이프 또는 가로 스크롤

#### Legal (`/terms`, `/privacy`, `/location-terms`)

- 정적 문서
- 이용약관 제2조: 서비스 성격 명확 정의

---

## 5. 컴플라이언스 가드레일

### 5.1 금지 용어 → 대체어 매핑

#### 공인중개사법 관련

| ❌ 금지 | ✅ 대체 |
|---------|---------|
| 추천 | 분석 결과 안내 |
| 알선 | 정보 연결 |
| 중개 | 정보 분석 |
| 매물 추천 | 조건 맞춤 단지 탐색 |
| 최적 매물 | 분석 상위 단지 |
| 맞춤 매물 | 조건 부합 단지 |

#### CTA 용어

| ❌ 금지 | ✅ 대체 |
|---------|---------|
| 매물 문의 | 외부 매물 보러가기 |
| 상담 신청 | 외부 사이트에서 확인 |
| 중개사 연결 | 주변 중개사무소 정보 |
| 바로 문의 / 바로 연결 / 전화하기 | 외부 페이지로 이동 |

#### 금융 용어

| ❌ 금지 | ✅ 대체 |
|---------|---------|
| 대출 가능 | 예상 대출 범위 (참고용) |
| 대출 승인 가능 | 참고용 시뮬레이션 결과 |
| 예산 적정 | 참고 범위 이내 |
| 투자 수익률 | 과거 실거래가 변동 추이 |
| 대출 가능 보장 | (사용 금지 — 대체 없음) |
| 거래 성사 보장 | (사용 금지 — 대체 없음) |
| 투자 수익 보장 | (사용 금지 — 대체 없음) |

#### 치안 표현

| ❌ 금지 | ✅ 대체 |
|---------|---------|
| 범죄율 높음 | 안전 편의시설 현황 |
| 위험 지역 | 안전 인프라 보통 |
| 치안 열악 | CCTV·가로등 보강 여지 |
| 가장 안전한 | 안전 인프라 상위 |
| 최적 투자 확정 | (사용 금지 — 대체 없음) |
| 가장 안전한 지역 확정 | (사용 금지 — 대체 없음) |

### 5.2 안전 점수 표현 규칙

**허용 표현**:
- 섹션 타이틀: "안전 편의시설 현황"
- 비교: "서울시 평균 대비", "수도권 상위 N%"
- 지표: CCTV 수, 가로등 밀집도, 경찰서 거리, 비상벨 수
- 색상: 충분=파랑, 보통=앰버, 부족=**진회색**

**금지 표현**:
- "범죄율 높음", "위험 지역", "치안 열악", "치안 위험도"
- 원시 범죄 데이터 직접 노출
- 순위형 비하 (D등급, 하위 20%)
- 빨강 색상으로 "위험" 표현

### 5.3 면책 5접점 체크리스트

| # | 위치 | 톤 | 빈도 | 구현 |
|---|------|-----|------|------|
| ① | 랜딩 하단 (모든 페이지 푸터) | 소프트 | 상시 | "본 서비스는 공공데이터 기반 정보 분석 플랫폼입니다 · 부동산 중개·알선·자문 서비스가 아닙니다" |
| ② | 입력 플로우 시작 시 배너 | 중립 | 1회 | "입력하신 정보는 분석 목적으로만 사용되며 거래 연결에 사용되지 않습니다" |
| ③ | 결과 상단 메타 태그 | 컨텍스트 내장형 | 결과 확인 시 | `[📊 공공데이터 기반 분석 결과] [기준일 2026.02.01] [참고용 정보]` |
| ④ | 외부 링크 클릭 모달 | 명시적 | 매 클릭 | "본 서비스는 정보 분석 플랫폼이며 부동산 중개 서비스가 아닙니다. 매물 정보의 정확성은 해당 외부 사이트에서 확인해주세요." |
| ⑤ | 이용약관 | 법률 문서 | 동의 시 | 제2조: 서비스 성격 정의 (공인중개사법 제2조 인용) |

### 5.4 외부 링크 CTA 규칙

- CTA 버튼: "외부 매물 보러가기 ↗" + 대상 사이트명
- 클릭 시 모달: 이동 대상 명시 + 면책 + 확인/취소
- `target="_blank" rel="noopener noreferrer"` 필수
- 하단 부가 텍스트: "외부 사이트로 이동합니다"

### 5.5 재무 시뮬레이션 면책 규칙

- 예산 계산 결과에 항상 부가: "참고용 시뮬레이션이며 실제 대출 승인을 보장하지 않습니다"
- 비단정 표현 3-bucket: 안정적 / 다소 부담 / 적극적 (면책 병기)
- 금융 데이터 표시 시 출처·기준일 필수

---

## 6. Tailwind v4 구현 가이드

### 6.1 `@theme` 설정

> **구현 파일**: `docs/design-system/design-tokens.css` 참조

Tailwind v4는 JavaScript 설정 파일 대신 CSS 내 `@theme` 지시어로 디자인 토큰을 정의한다.

```css
@import "tailwindcss";
@theme {
  --font-sans: "Pretendard Variable", "Pretendard", "Noto Sans KR", system-ui, sans-serif;

  --color-brand-50:  #CFFAFE;
  --color-brand-100: #A5F3FC;
  --color-brand-200: #67E8F9;
  --color-brand-300: #22D3EE;
  --color-brand-400: #06B6D4;
  --color-brand-500: #0891B2;
  --color-brand-600: #0E7490;
  --color-brand-700: #155E75;
  --color-brand-800: #164E63;
  --color-brand-900: #0C4A6E;
  /* ... 전체 토큰은 design-tokens.css 참조 */
}
```

### 6.2 Semantic Token Layer (Light/Dark)

```css
:root {
  --color-surface: #FFFFFF;
  --color-surface-elevated: #FAFAF9;
  --color-on-surface: #1C1917;
  --color-on-surface-muted: #78716C;
  --color-border: #E7E5E4;
  --color-primary: var(--color-brand-500);
}
.dark {
  --color-surface: #0F172A;
  --color-surface-elevated: #1E293B;
  --color-on-surface: #F1F5F9;
  --color-on-surface-muted: #94A3B8;
  --color-border: #334155;
  --color-primary: var(--color-brand-400);
}
```

컴포넌트에서 `bg-surface text-on-surface border-border`만 사용하면 라이트/다크 자동 전환.

### 6.3 컴포넌트별 유틸리티 클래스 패턴

**카드 기본**:
```
rounded-2xl bg-surface-elevated p-4 shadow-sm border border-border
```

**CTA 버튼**:
```
w-full min-h-12 rounded-xl bg-primary text-on-primary font-semibold
```

**점수 바**:
```
w-full h-2 rounded-full bg-brand-100 overflow-hidden
```

**터치 타겟 아이콘 버튼**:
```
min-h-11 min-w-11 flex items-center justify-center
```

**하단 고정 CTA**:
```
fixed bottom-0 inset-x-0 bg-surface/80 backdrop-blur-lg px-4 pt-3 pb-[max(12px,env(safe-area-inset-bottom))]
```

---

## 7. 소스 충돌 해소 기록

| # | 충돌 항목 | 소스 A | 소스 B | 해소 결정 | 근거 |
|---|----------|--------|--------|-----------|------|
| 1 | Tab bar 활성색 | step7: `#2563EB` (Blue 600) | design_brief: `#0891B2` (Warm Teal) | **`#0891B2` 채택** | brand primary 일관성, step3/design_brief 다수 문서와 일치 |
| 2 | 검색 단계 수 | step7: 3-step URL routing | step4: 5 UI sub-step | **5 UI sub-step in `/search`** | UI 관점 우선, URL은 단일 경로 |
| 3 | 지도 API | step3: Naver Maps 언급 | PHASE1 S1: Kakao Maps | **Kakao Maps 고정** | PHASE1 SoT 준수 |
| 4 | Card padding | PHASE1 S7: 24px | step3/design_brief: 16px (p-4) | **16px 채택** | 리서치 결과 우선 |
| 5 | Primary color | PHASE1 S7: `#1E40AF` | step3/design_brief: `#0891B2` | **`#0891B2` 채택** | 리서치 결과 우선 |

---

> **출처 문서** (Legacy, 읽기 전용):
> `design_brief.md`, `step1_competitive_service_analysis.md`, `step2_ux_principle.md`, `step3_design_system_basic.md`, `step4_onboarding_design_guide.md/.jsx`, `step5_result_design_guide.md/.jsx`, `step6_compliance_ux_design_guide.md/.jsx`, `step7_page_structure_routing.md`
> (경로: `docs/legacy_docs/references/design_research/`)
