# PHASE 4: SHIP & LEARN

> KPI 정의/게이트 수치는 PHASE0 S2 참조 — 여기에 복사하지 않습니다.
> 테스트 결과는 PHASE3 참조.

## 1. Release Checklist

| #   | 항목                                              | 상태    |
| --- | ------------------------------------------------- | ------- |
| 1   | Vercel 프로덕션 배포 완료                         | pending |
| 2   | 환경변수 설정 (DB/API 키/Redis)                   | pending |
| 3   | HTTPS/TLS 적용 확인                               | pending |
| 4   | 커스텀 도메인 설정 (선택)                         | pending |
| 5   | DB 마이그레이션 프로덕션 적용                     | pending |
| 6   | 초기 데이터 적재 완료                             | pending |
| 7   | 이벤트 트래킹 동작 확인 (→ PHASE0 S2 이벤트 10개) | pending |
| 8   | 이용약관/개인정보처리방침 페이지 접근 가능        | pending |
| 9   | 면책 문구/출처 표기 최종 확인                     | pending |
| 10  | README 최종 업데이트                              | pending |

## 2. Smoke Test (프로덕션)

배포 직후 수동으로 확인하는 기본 동작 테스트:

| #   | 시나리오                 | 예상 결과                    | 상태    |
| --- | ------------------------ | ---------------------------- | ------- |
| 1   | 랜딩 페이지 접속         | 정상 렌더링 + 면책 고지 노출 | pending |
| 2   | 최소 입력 → 결과 조회    | Top10 결과 + 지도 마커 표시  | pending |
| 3   | 결과 카드 점수 분해 확인 | 5개 지표 + 출처/기준일 표시  | pending |
| 4   | 외부 매물 아웃링크 클릭  | 새 탭으로 외부 사이트 이동   | pending |
| 5   | 동의 UI 노출/체크        | 필수/선택 분리 동작          | pending |
| 6   | 이용약관 페이지 접근     | 정상 표시                    | pending |
| 7   | 모바일 반응형            | 레이아웃 정상                | pending |

## 3. Rollback Policy

### 트리거 조건

| 트리거           | 임계치        | 최소 표본        | 즉시 조치                           |
| ---------------- | ------------- | ---------------- | ----------------------------------- |
| 5xx 에러율       | >= 5% (5분간) | 5분간 요청 >= 50 | 이전 배포로 Vercel instant rollback |
| API 응답시간 p95 | > 5초 (5분간) | 5분간 요청 >= 50 | 이전 배포로 rollback                |
| DB 연결 실패     | 연속 3회      | -                | 점검 모드 전환 + 이전 배포 rollback |

### 롤백 절차

1. Vercel Dashboard > Deployments > 이전 배포 "Promote to Production"
2. 원인 분석 후 아래 Learning Log에 기록
3. 수정 후 재배포

## 4. Metrics

→ **PHASE0 S2 참조**

KPI 판정 기준: 포트폴리오 경량 기준 (`result_view >= 100건` + 7일 관찰)

> KPI 정의, 이벤트명, 분모/분자, 기준값은 PHASE0 S2에 정의되어 있습니다.
> 여기에 중복 복사하지 않습니다.

## 5. Learning Log

> KPI 판정 기간 완료 후 아래 섹션을 작성합니다.

### 5.1 KPI 결과 (목표 vs 실제)

| KPI              | 목표     | 실제 | 판정 |
| ---------------- | -------- | ---- | ---- |
| 컨시어지 CTR     | >= 20%   | -    | -    |
| 입력 완료율      | >= 50%   | -    | -    |
| 결과-문의 전환율 | go >= 8% | -    | -    |

### 5.2 회고 (Keep / Improve / Stop)

## **Keep** (잘한 것, 유지할 것):

## **Improve** (개선할 것):

## **Stop** (그만할 것):

### 5.3 다음 사이클 액션

| 우선순위 | 액션 | 근거 |
| -------- | ---- | ---- |
| -        | -    | -    |
